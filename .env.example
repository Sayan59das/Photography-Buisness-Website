# Postgres connection string used by Prisma (src/lib/prisma.ts, prisma/schema.prisma).
# Provision one via Vercel Marketplace (Neon / Prisma Postgres) or your own Postgres instance.
DATABASE_URL="postgresql://user:password@host:5432/dbname"
